Pull the latest claude.me config from GitHub and re-run the install script to apply any updates.

Run this bash command:
```bash
cd ~/claude.me && git pull && bash install.sh
```

After running, confirm to the user which files were updated/symlinked and whether any backups were created.
